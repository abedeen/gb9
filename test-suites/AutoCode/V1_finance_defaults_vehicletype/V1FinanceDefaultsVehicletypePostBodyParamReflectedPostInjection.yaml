---
parent: V1_finance_defaults_vehicletype
name: V1FinanceDefaultsVehicletypePostBodyParamReflectedPostInjection
description: Returns default finance data
authors:
- apisec Bot
type: SUITE
init: []
localVariables: []
path: /v1/finance/defaults/{VehicleType}
endpoint: /v1/finance/defaults/NEW
method: POST
auth: Default
headers:
- 'Accept: application/json'
- 'Content-Type: application/json'
- 'Accept: {{@Var.Accept}}'
testCases:
- id: 1
  body: |-
    {
      "Criteria" : {
        "Key" : "{{@Injection}}",
        "Name" : "{{@Injection}}",
        "Type" : "{{@Injection}}",
        "IsDefault" : false,
        "Term" : {
          "Options" : [ {
            "IsDefault" : false,
            "Value" : "{{@RandomInteger}}"
          } ]
        },
        "Deposit" : {
          "Default" : "{{@Injection}}",
          "IsRequired" : false,
          "Minimum" : "{{@Injection}}",
          "Maximum" : "{{@Injection}}",
          "DefaultIsPercentage" : false,
          "Locale" : {
            "DisplayValue" : "{{@Injection}}"
          },
          "OrderNo" : "{{@Injection}}",
          "MaximumAsPercentageOfOtr" : "{{@Injection}}",
          "MinimumAsPercentageOfOtr" : "{{@Injection}}",
          "DefaultAsPercentageOfOtr" : "{{@Injection}}"
        },
        "Mileage" : {
          "Options" : [ {
            "IsDefault" : false,
            "Value" : "{{@RandomInteger}}"
          } ]
        },
        "AdvanceRentals" : {
          "Options" : [ {
            "IsDefault" : false,
            "Value" : "{{@RandomInteger}}"
          } ]
        },
        "MonthlyPrice" : {
          "Min" : "{{@Var.Min}}",
          "Max" : "{{@Var.Max}}"
        },
        "IsPersonalised" : false,
        "RegularPayment" : "{{@Injection}}",
        "PartExchange" : "{{@Injection}}",
        "Settlement" : "{{@Injection}}",
        "CustomerType" : "{{@Injection}}",
        "VehicleType" : "{{@Injection}}"
      },
      "Vin" : "{{@Injection}}",
      "OfferPrice" : "{{@Var.OfferPrice}}",
      "VehicleType" : "{{@Injection}}"
    }
  inactive: false
useCategoryAssertion: false
assertions:
- '@Response !=~ (?s:.)*<(?s:.*)(injectionReflection)(?s:.*)>(?s:.*)'
cleanup: []
severity: High
category: reflected_post_injection
tags:
- V1
- OWASP-API7
inactive: false
policies:
  initExec: Request
  cleanupExec: Request
  logger: DEBUG
  timeoutSeconds: 300
  repeatOnFailure: 0
  repeat: 0
  repeatModule: '@APIsec/Common/reflected_post_injection'
  repeatDelay: 0
  unmaskToken: false
  postExecutionDelay: 0
  showResponse: true
autoGenerated: true
